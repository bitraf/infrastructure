server {
    listen 80 default_server;
    listen [::]:80 default_server;

    index index.html;

    location /index.html {
        root /var/www/p2k16-staging.bitraf.no;
        try_files $uri $uri/ =404;
    }
}
