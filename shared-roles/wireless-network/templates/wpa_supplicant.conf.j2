# managed with ansible
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
p2p_disabled=1
country={{ wifi__country }}

network={
    #scan_ssid=1
    ssid="{{ wifi_ssid }}"
    psk="{{ wifi_psk }}"
}
