

install_path: /var/www/html/
user: www-data
site_name: test
base_url: https://wiki.bitraf.no
domain: www.bitraf.no



# Which version of WordPress to deploy
wp_version: 4.9.8
wp_checksum: "sha1:0945bab959cba127531dceb2c4fed81770812b4f"

# These are the WordPress database settings
wp_db_name: wordpress
wp_db_user: wordpressuser
wp_db_password: ""

# You shouldn't need to change this.
mysql_port: 3306
wp_db_host: db.bitraf.no

# This is used for the nginx server configuration, but access to the
# WordPress site is not restricted by a named host.
server_hostname: www.bitraf.no

# Disable All Updates
# By default automatic updates are enabled, set this value to true to disable all automatic updates
auto_up_disable: false

#Define Core Update Level
#true  = Development, minor, and major updates are all enabled
#false = Development, minor, and major updates are all disabled
#minor = Minor updates are enabled, development, and major updates are disabled
core_update_level: true
